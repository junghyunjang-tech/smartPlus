# 환경 변수 예시 파일
# 실제 사용시에는 .env 파일로 복사하여 사용하세요

# Jasypt 암호화 키 (실제 운영 환경에서는 안전하게 관리 필요)
JASYPT_ENCRYPTOR_PASSWORD=your_secure_encryption_key_here

# 데이터베이스 설정 (호스트의 PostgreSQL 사용)
# 데이터베이스 URL (호스트의 PostgreSQL을 사용하는 경우)
DB_URL=jdbc:postgresql://host.docker.internal:5432/smartplus
DB_USERNAME=postgres
DB_PASSWORD=your_db_password_here
